<% if user_signed_in? %>
  <div class="flex flex-col items-center mt-8">
    <div class="flex justify-center mt-8">
      <%= image_tag 'redondo_violeta.png', alt: 'Enronda Logo', style: 'width: 600px; height: 600px; margin-top: -6rem;' %>
    </div>
  </div>
<% end %>
